-to install our application all you need to do is, clone the git repository,
-open the folder with terminal and type "python(or python3) manage.py runserver" to run the application.
-click on the local link that appears.
-add "/home" and voila, application works.